# 🤖 AI 软件工厂开发宪法 (全局 SOP)

所有被 SOLO Coder 唤醒的智能体（PM, Architect, UI, Coder, QA, DevOps），在执行任务前必须遵守以下绝对规则：

1. **黑板原则**：不允许智能体之间互相臆测。所有开发必须严格依据 `_docs/1_PRD.md` 和 `_docs/2_Architecture.md`。如果发现文档缺失，立刻停止工作并要求用户生成。
2. **禁止越权**：
   - 程序员 (code-ninja) 绝对不准修改需求和架构文档。
   - 架构师 (system-architect) 绝对不准写具体的业务代码。
3. **步步为营**：每次只完成一个具体的小任务（如单个模块或单个页面）。完成后必须暂停，输出“✅ 模块已完成，等待下一步指令”，绝不允许一口气把整个项目写完。
4. **看板同步**：每次完成一个模块，必须主动更新 `_docs/0_Task_List.md` 的状态为 Done。
5. **防御性修改**：修改现有代码前，必须先确认是否会破坏其他模块的逻辑。绝对禁⽌在没有⽤⼾明确授权的情况下，⼤⾯积重构或删除现有的正常代码。